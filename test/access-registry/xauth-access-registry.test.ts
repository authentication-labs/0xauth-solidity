import { ethers } from 'hardhat';

describe('xAuthAccessRegistry', function () {
  it('tests access registry', async () => {
    // const registryFactory = await ethers.getContractFactory('xAuthAccessRegistry');
    // registryFactory.deploy();
    // TBD
  });
});
